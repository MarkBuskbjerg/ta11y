import '../styles/_main.css';
import './helper.js';
import './navigationHelper.js';

if (DEV_MODE) {
	// DEV_MODE gets replaced at build time based on ENV!
	console.log('Dev mode is currently enabled.');
}
